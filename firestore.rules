rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // Asegura que solo los usuarios autenticados puedan leer o escribir
    match /{document=**} {
      allow read, write: if request.auth != null;
    }
  }
}
